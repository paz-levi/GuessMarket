<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee https://jakarta.ee/xml/ns/jakartaee/web-app_6_0.xsd"
         version="6.0">
    <!-- Every servlet is routed via its own @WebServlet annotation, not a servlet-mapping element here, so a
         servlet's URL lives in exactly one place (the class itself) rather than two files staying in sync. -->
    <display-name>Guess Market</display-name>

    <!-- Server restart wipes all state per the Ex3 spec (no persistence); 30 minutes is generous for a single
         grading/testing session and simply expires stale demo sessions rather than encoding any real requirement. -->
    <session-config>
        <session-timeout>30</session-timeout>
    </session-config>
</web-app>
